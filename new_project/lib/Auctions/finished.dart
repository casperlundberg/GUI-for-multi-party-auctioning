import 'package:flutter/material.dart';

class Finished extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SizedBox(
      width: 325.0,
      height: 700.0,
      child: const Card(child: Text('Finished')),
    );
  }
}
